name: Hello world demo
on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-22.04
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: print Hello
        run: |
          set -e
          echo "[INFO] hello, Github Actions!"
